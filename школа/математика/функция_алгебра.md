- это правило, которое каждому элементу из одного множества([[Область определения]]) ставит в соответствие **ровно один** элемент другого множества([[Область значений]])
- обозначают символом `f`, а её значение при аргументе `X` записывают так `f(X)`
- #алгебра 